<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <meta content="<%= I18n.t 'content.tags'[0..244] %>" name="keywords" />
    <meta content="This is Alexandr Basan's website who love Ruby on Rails, web development and good technologies." name="description">
    <meta name="author" content="Alexandr Basan" />
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
    <%= favicon_link_tag 'bl_16_16.png' %>
    <script src="http://cdnjs.cloudflare.com/ajax/libs/waypoints/2.0.3/waypoints.min.js"></script>
    <script src="/assets/jquery.counterup.min.js"></script>
    <script>
        jQuery(document).ready(function($) {
            $('.counter').counterUp({
                delay: 10,
                time: 1000
            });
        });
    </script>

<!--    <script>
        $("img.lazy").each(function() {
            $(this).attr("data-original",$(this).attr("src"));
            $(this).removeAttr("src");
        });
        $("img.techno-img").each(function() {
            $(this).attr("data-original",$(this).attr("src"));
            $(this).removeAttr("src");
        });
        $("img.techno-img2").each(function() {
            $(this).attr("data-original",$(this).attr("src"));
            $(this).removeAttr("src");
        });
    </script>
    <script>
        $(function() {
            $("img.lazy").lazyload();
            $("img.techno-img").lazyload();
            $("img.techno-img2").lazyload();
        });
    </script>-->

    <!--Google Analytics-->
    <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
            (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
                m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

        ga('create', 'UA-27225128-1', 'auto');
        ga('send', 'pageview');

    </script>
    <!--Google Analytics-->
  </head>
  <body>
    <%= render 'layouts/header' %>
    <%= content = yield %>
    <!--Back To Top Button-->
    <% if content.length > 1200 %>
      <%= link_to("#top") do %>
          <div id="backtotop" style="display: block;">
          </div>
      <% end %>
  <% end %>
  <% if current_user.try(:admin?) %>
      <%= render 'layouts/admin' %>
  <% else %>
  <% end %>
      <div class="container">
      <%= debug(params) if Rails.env.development? %>
      </div>
  </body>
</html>